import styles from "./Footer.module.css";

export default function Footer() {
  return (
    <section id="footer" className={styles.footerSectionContainer}>
      <main className={styles.footerSection}>
        footer SECTION
      </main> 
    </section>
  );
}